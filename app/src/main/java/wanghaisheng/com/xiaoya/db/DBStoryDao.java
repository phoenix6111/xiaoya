package wanghaisheng.com.xiaoya.db;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import wanghaisheng.com.xiaoya.db.DBStory;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "DBSTORY".
*/
public class DBStoryDao extends AbstractDao<DBStory, Long> {

    public static final String TABLENAME = "DBSTORY";

    /**
     * Properties of entity DBStory.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Title = new Property(1, String.class, "title", false, "TITLE");
        public final static Property Theme_id = new Property(2, Integer.class, "theme_id", false, "THEME_ID");
        public final static Property Name = new Property(3, String.class, "name", false, "NAME");
        public final static Property Image = new Property(4, String.class, "image", false, "IMAGE");
        public final static Property Images = new Property(5, String.class, "images", false, "IMAGES");
        public final static Property Body = new Property(6, String.class, "body", false, "BODY");
        public final static Property Css = new Property(7, String.class, "css", false, "CSS");
        public final static Property Share_url = new Property(8, String.class, "share_url", false, "SHARE_URL");
        public final static Property Is_collected = new Property(9, Boolean.class, "is_collected", false, "IS_COLLECTED");
    };


    public DBStoryDao(DaoConfig config) {
        super(config);
    }
    
    public DBStoryDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"DBSTORY\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"TITLE\" TEXT," + // 1: title
                "\"THEME_ID\" INTEGER," + // 2: theme_id
                "\"NAME\" TEXT," + // 3: name
                "\"IMAGE\" TEXT," + // 4: image
                "\"IMAGES\" TEXT," + // 5: images
                "\"BODY\" TEXT," + // 6: body
                "\"CSS\" TEXT," + // 7: css
                "\"SHARE_URL\" TEXT," + // 8: share_url
                "\"IS_COLLECTED\" INTEGER);"); // 9: is_collected
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"DBSTORY\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, DBStory entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String title = entity.getTitle();
        if (title != null) {
            stmt.bindString(2, title);
        }
 
        Integer theme_id = entity.getTheme_id();
        if (theme_id != null) {
            stmt.bindLong(3, theme_id);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(4, name);
        }
 
        String image = entity.getImage();
        if (image != null) {
            stmt.bindString(5, image);
        }
 
        String images = entity.getImages();
        if (images != null) {
            stmt.bindString(6, images);
        }
 
        String body = entity.getBody();
        if (body != null) {
            stmt.bindString(7, body);
        }
 
        String css = entity.getCss();
        if (css != null) {
            stmt.bindString(8, css);
        }
 
        String share_url = entity.getShare_url();
        if (share_url != null) {
            stmt.bindString(9, share_url);
        }
 
        Boolean is_collected = entity.getIs_collected();
        if (is_collected != null) {
            stmt.bindLong(10, is_collected ? 1L: 0L);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public DBStory readEntity(Cursor cursor, int offset) {
        DBStory entity = new DBStory( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // title
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // theme_id
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // name
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // image
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5), // images
            cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6), // body
            cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7), // css
            cursor.isNull(offset + 8) ? null : cursor.getString(offset + 8), // share_url
            cursor.isNull(offset + 9) ? null : cursor.getShort(offset + 9) != 0 // is_collected
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, DBStory entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setTitle(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setTheme_id(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setName(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setImage(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setImages(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
        entity.setBody(cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6));
        entity.setCss(cursor.isNull(offset + 7) ? null : cursor.getString(offset + 7));
        entity.setShare_url(cursor.isNull(offset + 8) ? null : cursor.getString(offset + 8));
        entity.setIs_collected(cursor.isNull(offset + 9) ? null : cursor.getShort(offset + 9) != 0);
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(DBStory entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(DBStory entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
